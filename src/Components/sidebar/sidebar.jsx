import styles from "./sidebar.module.css"

const Sidebar = () => {
    return (
        <article className={`${styles.contents} mainpanel`}>
            <main className={styles.sidebarbody}>

            </main>
        </article>
    )
}

export default Sidebar